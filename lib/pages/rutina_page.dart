import 'package:flutter/material.dart';

class RutinaPage extends StatelessWidget {
  const RutinaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Rutinas próximamente"),
    );
  }
}
