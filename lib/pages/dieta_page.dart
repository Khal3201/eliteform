import 'package:flutter/material.dart';

class DietaPage extends StatelessWidget {
  const DietaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Dietas próximamente"),
    );
  }
}
